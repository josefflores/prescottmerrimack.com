/**
 *  @name   sendmail
 *
 *  This function sends a contact form email
 *
 *  @return 0   Success
 *  @return 1   bad form
 *  @return 2   api error
 */
function sendMail( ) {
    var name = $( '#contactName' ).val() ;
    var email = $( '#contactEAddress' ).val() ;
    var subject = $( '#contactSubject' ).val() ;
    var text = $( '#contactEmail' ).val() ;


} ;

